El baseline se reutiliza de `20260921T222953Z_e54cd3c9`, medido en la misma sesión y con
los mismos evaluadores. Ninguno de los dos arreglos posteriores (middleware de
respuesta estructurada y regla de huecos del prompt) toca su código: el baseline
se construye desde `src/Baseline_Agente_10K.ipynb` con su propio SYSTEM y sin
middleware. Sólo se ha vuelto a medir el agente final.
